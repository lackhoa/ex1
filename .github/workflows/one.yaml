name: One

env:
  X: 5

on:
  push:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      message:
        required: true
        default: 'hello world'

jobs:
  a:
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
    - run: |
        exit 1
