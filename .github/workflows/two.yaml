name: two

on:
  workflow_call:

jobs:
  a:
    name: two-a
    runs-on: ubuntu-latest
    steps:
    - run: exit 0
    - uses: actions/upload-artifacts@v3
      with:
        name: two
        path: .
