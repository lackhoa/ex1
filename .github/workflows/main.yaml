name: Main

on:
  push:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      message:
        required: true
        default: 'hello world'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo $GH | jq
        env:
          GH: ${{ toJSON(github) }}
