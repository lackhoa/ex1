name: Main

on:
  push:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      message:
        required: true
        default: 'hello world'

jobs:
  build:
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - run: |
          ./cleanup-old-amis.py
