name: Test workflow

on:
  push:
    branches:
      - '**'

jobs:
  build:
    outputs:
      key: ${{ true == true }}
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v2
      if: false
      with:
        token: ${{ secrets.PAT }}

    - name: one
      run: |
        echo X=1 >> ${GITHUB_ENV}
        echo $X
        echo ${{ env.X }}

    - name: two
      run: echo $X
